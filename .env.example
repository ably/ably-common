# GitHub API Token for fetching releases (optional but recommended for rate limits)
GITHUB_TOKEN=your_github_token_here

# OpenAI API Key for summarizing release notes (optional)
OPENAI_API_KEY=your_openai_api_key_here

# AWS Configuration for S3 uploads
# Note: AWS_ROLE_ARN and AWS_REGION are used in GitHub Actions CI only (set as repository secrets)
# For local development, use AWS CLI profiles or environment variables
S3_BUCKET=schemas.ably.com
S3_PREFIX=csv/agents

# CI-only secrets (set these in GitHub repository secrets, not in .env):
# AWS_ROLE_ARN - IAM role for GitHub Actions OIDC authentication
# AWS_REGION - AWS region for S3 bucket (e.g., us-east-1)
